proxmox_host = "Domain name of proxmox host"
proxmox_api_token_id = "Username or token ID to authenticate against proxmox API"
proxmox_api_password = "Password to authenticate against proxmox API"
proxmox_node = "name of node to create VM template on"
ssh_username = "Username to use when connect to VM over ssh"
ssh_public_key = [
    "SSH public keys to use to SSH into the created VM"
]